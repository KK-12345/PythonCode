def selection_sort(a):
    least = 0
    for i in range(length):
        for k in range(i+1,, length)
            if a[i] < a[least]:
                least = k
            # swap elements


def insertion_sort(a):